struct BinaryNode{
	object element; // data (in bubble)
	BinaryNode *left; // left child
	BinaryNode *right; // right child
};

// leaf has no *left or *right pointers
// constructed with 'new's and recursion
// root is pointer to top node


